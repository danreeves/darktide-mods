---
base: lua51 # We will be extending off of Lua 5.1.
globals:
  RTC.connect:
    args:
      - type: string
      - type: function
      - type: function
      - type: function
  RTC.send:
    args:
      - type: string
      - type: string
      - type: string
  RTC.disconnect:
    args:
      - type: string
