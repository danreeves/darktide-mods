---
base: lua51 # We will be extending off of Lua 5.1.
globals:
  WebSockets.connect:
    args:
      - type: string
      - type: function
  WebSockets.send:
    args:
      - type: any
      - type: string
  WebSockets.close:
    args:
      - type: any
